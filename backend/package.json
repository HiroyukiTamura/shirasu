{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "run script": "ts-node ./src/index.ts",
    "tsc": "tsc",
    "run browserify(please run on terminal after run tsc)": "browserify dist -p esmify > bundle.js"
  },
  "author": "",
  "dependencies": {
    "@auth0/auth0-spa-js": "^1.13.1"
  },
  "devDependencies": {
    "browserify": "^17.0.0",
    "esmify": "^2.1.1",
    "tsify": "^5.0.2",
    "typescript": "^4.0.5"
  }
}
